# Moved

This file has moved to the shipped gemba repo:

**Canonical location:** `gemba/docs/ui-spec.md`

On disk (for this workstation): `~/gt/gemba/docs/ui-spec.md` (or `~/gt/gemba/crew/<rig>/docs/ui-spec.md` inside any crew rig).

The move closed the `docs/ui-spec.md` deliverable of `gm-p27` (UI design-review gate). Update any remaining pointers to the new path; edits to the spec now live in the gemba repo so they ship alongside the code they govern.
